1. Create and switch to a new branch

```bash
git checkout -b version-1
```

2. Verify on right branch

```bash
git branch
```

3. Add, commit and push

```bash
git add .
git commit -m "add shop models - product, order, custom order"
git push -u origin version-1
```